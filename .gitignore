# Personal reference docs — local only, per request
docs/SETUP.md
docs/COMMANDS.md
PERFORMANCE.md

# Secrets — never commit
.env
.env.local
*.pem
client_secret*.json

# Local runtime state: Postgres data, zero-cache SQLite replica, FTS index
.data/

# Node
node_modules/
dist/
*.tsbuildinfo

# C++ / CMake
engine/build/
compile_commands.json

# Xcode
app/build/
*.xcuserstate
xcuserdata/
DerivedData/

# macOS
.DS_Store

# Swift build output
app/.build/
app/.swiftpm/

# Assembled app bundle (output of scripts/build-app.sh)
build/

# Generated by build-app.sh from packages/client/dist. The path said
# `Sources/Mail/` until 2026-08-19, which was the pre-rename spelling and
# matched nothing — so the built bundle was tracked for months against the
# intent of this very rule.
app/Sources/Alap/Web/

# Local signing identity export (scripts/make-signing-cert.sh)
*.p12

# Design review working docs — local only
DESIGN-REVIEW.md
DESIGN-REVIEW-MOTION.md
DESIGN-REVIEW-INSTRUMENT.md

# Local planning doc, not part of the project history.
SANITIZATION_PLAN.md
