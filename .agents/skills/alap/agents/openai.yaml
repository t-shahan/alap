interface:
  display_name: "Alap"
  short_description: "Repo-specific patterns and workflows for alap"
  default_prompt: "Use the alap repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true